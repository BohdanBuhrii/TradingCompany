<Page x:Class="TradingCompanyWPF.CategoryManager.CategoryList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TradingCompanyWPF.CategoryManager"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CategoryList">

    <Grid>

        <DataGrid 
            ItemsSource="{Binding Categories}"
            SelectedItem="{Binding SelectedCategory}"
            
            x:Name="CategoryGrid"
            AutoGenerateColumns="False"
            
            >
            <DataGrid.Columns>
                <DataGridTextColumn Header="id" Binding="{Binding Id}" Width="90" />
                <DataGridTextColumn Header="name" Binding="{Binding Name}" Width="80" />
                <DataGridTextColumn Header="isActive" Binding="{Binding isActive}" Width="50" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
